export type LoginUserTypes  = {
    email: string
    employee_id: string
    owner_id: string
    password: string
  }

  export type updateProductType = {
    id?: string
    name: string,
    price: string,
    status?: string,
    quantity: number
  }
  